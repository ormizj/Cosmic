const config = {};

config.USE_DISABLE_MAX_LEVEL = true // Sets max level for every PQ to the max level
config.USE_DISABLE_MIN_PLAYERS = true // Sets the requirement minimum players for every PQ to 1

module.exports = config;